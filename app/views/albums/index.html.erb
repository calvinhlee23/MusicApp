<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>List of All Albums</h1>
    <ul>
      <% @albums.each do |a| %>
      <%= link_to "#{a.title}", album_url(a) %>
      <% end %>
    </ul>
  </body>
</html>
