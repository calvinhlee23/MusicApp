<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>List of All tracks</h1>
    <ul>
      <% @tracks.each do |a| %>
      <%= link_to "#{a.title}", track_url(a) %>
      <% end %>
    </ul>
  </body>
</html>
